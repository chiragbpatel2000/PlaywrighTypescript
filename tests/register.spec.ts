import {expect, test} from '@playwright/test';

test('register functionality test',async({page})=>{

    await page.goto('https://automationteststore.com/');
  //  await expect(page).toHaveTitle(/.*checkout/);

})